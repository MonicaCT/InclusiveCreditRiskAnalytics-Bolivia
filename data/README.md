# Data

## Raw data

`data/raw/` contains the original Excel workbooks from the legacy portfolio repository. They are retained as source artifacts.

## Processed data

`data/processed/` contains branch-level analytical panels generated by `scripts/01_run_analysis.R`.

## Privacy note

The raw workbooks include an officer/personnel sheet. Public processed outputs intentionally exclude personal names and use branch-level aggregates.
